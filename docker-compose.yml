services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: mfcms
    ports:
      - "5432:5432"
    volumes:
      - mfcms_pg:/var/lib/postgresql/data

volumes:
  mfcms_pg:
